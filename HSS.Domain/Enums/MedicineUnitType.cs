namespace HSS.Domain.Enums
{
    public enum MedicineUnitType // الوحدة اللي هيتباع بيه الدواء 
    {
        Tablet,       // Single tablet
        Box,          // Box containing multiple items
        Strip,        // Strip of tablets/capsules
        Bottle,       // Bottle for liquids or syrups
        Ampoule,      // Single-dose injectable liquid
        Vial,         // Multi-dose injectable liquid
        Tube,         // Creams, gels, or ointments
        Sachet,       // Single-dose packets (e.g., powders or granules)
        Suppository,  // Single suppository
        Dropper,      // Eye drops or similar
        Syringe,      // Pre-filled syringe
        Inhaler,      // Devices for inhaled medications
        Patch,        // Transdermal patches
        Jar,          // Containers for creams or powders
        Cartridge,    // Medication cartridges (e.g., insulin pens)
        Nebule        // Small vial for nebulizer use
    }

}
